// No real production deployment target exists yet for this portfolio project.
// Defaulting to a relative base URL assumes the frontend is served from the same
// origin as the API Gateway (e.g. behind a shared reverse proxy) - update this
// once a real deployment target is known.
export const environment = {
  production: true,
  apiBaseUrl: ''
};
